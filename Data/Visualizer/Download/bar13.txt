{"id": 13, "style": "Bar", "contentVariable": "", "contentFunction": "mediump vec3 hsv(mediump float h,mediump float s,mediump float v) {\n    return mix(vec3(1.),clamp((abs(fract(h+vec3(3.,2.,1.)/3.)*6.-3.)-1.),0.,1.),s)*v;\n}", "contentVisualizer": "uv = gl_FragCoord.xy / iResolution.xy;\n    const mediump float bands = 40.0;\n    mediump vec2 p;\n    p.x = floor(uv.x*bands)/bands;\n    p.y = uv.y;\n    mediump float fft  = texture2D( iChannel0, vec2(p.x,0.0) ).x;\n    mediump vec3 color = hsv(p.x, 1.0 - p.y, 1.0);\n    mediump float dx = fract( (uv.x - p.x) * bands) - 0.5;\n    mediump float led = smoothstep(0.5, 0.3, abs(dx));\n    mediump float mask = (p.y < fft) ? 1.0 : 0.0;\n    visualizer = vec3(color*mask*led);"}