{"id": 6, "style": "Magic", "contentVariable": "", "contentFunction": "", "contentVisualizer": "mediump float a = iResolution.x/iResolution.y;\n    uv = gl_FragCoord.xy/iResolution.xy;\n    uv.x *= a;\n    mediump vec2 q = uv - vec2(0.5*a, 0.5);\n    mediump float fft = texture2D(iChannel0, vec2(0.0, 0.)).x;\n    mediump float fft2 = texture2D(iChannel0, vec2(1., 0.)).x;\n    mediump float t = atan(q.y, q.x);\n    mediump float ts = 0.2;\n    mediump float r = pow(fft*2.-0.5, 2.)*cos(6.*t + (fft2*1.5 + ts)*5.) + 2.0 + pow(fft, 2.);\n    r *= fft/12. + 0.04;\n    visualizer = vec3(smoothstep(r, r + fft*0.02 + 0.01, length(q)));\n    visualizer *= 1.*smoothstep(0., r + fft*0.2, length(q));\n    visualizer.r = clamp(visualizer.r, 0.0, 1.0);\n    visualizer.g = clamp(visualizer.g, 0.0, 1.0);\n    visualizer.b = clamp(visualizer.b, 0.0, 1.0);\n    mediump vec3 col2 = 1.0 - visualizer;\n    col2 *= vec3(1.0, 1.0, 1.0);\n    visualizer = col2;"}