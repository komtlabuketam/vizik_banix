{"id": 3, "style": "Static", "contentVariable": "", "contentFunction": "mediump float line(mediump float top, mediump float bottom, mediump float sharpen, mediump vec2 uv) {\n    sharpen = 0.05 * smoothstep(0.6, 1., uv.x) + 0.05 * smoothstep(0.3, 0., uv.x);\n    return smoothstep(top, top + sharpen, uv.y) - smoothstep(bottom, bottom + sharpen, uv.y);\n}\n\nmediump float wave(mediump float time, mediump vec2 uv, mediump float phase) {\n    mediump float wave = sin(time + uv.x * phase);\n    mediump float blur = 0.1 * smoothstep(0., .5, uv.x) * 0.1 * smoothstep(1., 0.5, uv.x);\n    uv.y += phase * blur * wave;\n    return line(0.495, 0.505, 0.01, uv);\n}", "contentVisualizer": "uv = gl_FragCoord.xy/iResolution.xy;\n    mediump float audio = 1.;\n    mediump float line = wave(iTime * 3., uv, 10. + sin(iTime) * 1.5);\n    mediump float line2 = wave(iTime * 4., uv, 13. + sin(iTime + 2.) * 3.);\n    mediump float line3 = wave(iTime *  5., uv, 20. + sin(iTime + 3.) * 3.);\n\n    mediump vec3 col1 = vec3(1.0, 0.1, 0.2) * line;\n    mediump vec3 col2 = vec3(0.8, 0.4, 0.2) * line2;\n    mediump vec3 col3 = vec3(0.3, 0.2, 0.9) * line3;\n    mediump vec3 col = 4. * (col1  + col2  + col3 );\n    visualizer = col;"}