{"id": 2, "style": "Magic", "contentVariable": "", "contentFunction": "void disk(mediump vec2 r, mediump vec2 center, mediump float radius, mediump vec3 color, inout mediump vec3 pixel){\n    if(length(r-center) < radius){\n        pixel = color;\n    }\n}", "contentVisualizer": "uv = 2.0 * vec2(gl_FragCoord.xy - 0.5 * iResolution.xy) / iResolution.y;\n    mediump vec3 bgColor = vec3(0.5);\n    mediump vec3 col1 = vec3(0);\n    mediump float fft = texture2D(iChannel0, vec2(abs(uv.y*uv.x), 0.075)).x;\n    mediump float wave = texture2D(iChannel0, vec2(uv.x, .0075)).x;\n    mediump vec3 col = vec3( fft, 4.0*fft*(1.0-fft), 1.0-fft ) * fft;\n    col += 1.0 - smoothstep(0.0,0.15, abs(wave - uv.y));\n    mediump vec3 pixel = vec3(fft, fft, fft);\n    disk(uv, vec2(0,0), fft, col1, pixel);\n    visualizer = pixel;"}