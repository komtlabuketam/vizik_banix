{"id": 15, "style": "Bar", "contentVariable": "#define pi 3.14159265359\n#define NUM_COLUMNS 32.0\n#define NUM_ROWS 32.0\n#define BAR_HEIGHT 2\n#define BAR_WIDTH 2", "contentFunction": "int modul(mediump float lhs, mediump float rhs) {\n    return int(lhs - rhs * floor(lhs / rhs));\n}", "contentVisualizer": "uv = gl_FragCoord.xy / iResolution.xy;\n    mediump float discX ;\n\n    discX = floor(uv.x * NUM_COLUMNS) / NUM_COLUMNS;\n\n    mediump float freq = texture2D(iChannel0, vec2(discX, 0.0)).x;\n    mediump float wave = texture2D(iChannel0, vec2(discX, 1.0)).x;\n\n    mediump float discFrequency = floor(freq * NUM_ROWS) / NUM_ROWS;\n    mediump vec4 color = vec4(0.0, 0.0, 0.0, 1.0);\n\n    mediump int blackBarV = modul(gl_FragCoord.y, iResolution.y / NUM_COLUMNS);\n    mediump int blackBarH = modul(gl_FragCoord.x, iResolution.x / NUM_ROWS);\n\n\n    if(discFrequency > uv.y && blackBarV > BAR_HEIGHT && blackBarH > BAR_WIDTH) {\n        visualizer = vec3((1.0 - uv.x) * freq, freq * uv.x, uv.y);\n    }"}