{"id": 2,"style": "Spectrum","contentVariable": "","contentFunction": "","contentVisualizer": "uv= (gl_FragCoord.xy-.5*iResolution.xy)/min(iResolution.x, iResolution.y);\n    uv = vec2( 2.0*length(uv), atan(abs(uv.y), abs(uv.x)) );\n    mediump float value, fft, wave;\n    value = abs(uv.x-0.5);\n    fft  = texture2D( iChannel0, vec2(value,0.25) ).x;\n    wave = texture2D( iChannel0, vec2(value,0.0) ).x;\n    visualizer = vec3( 4.0*fft*(1.0-fft)*(0.2+0.4*sin(iTime))\n    , fft+0.3*sin(0.22*iTime)\n    , 0.5-fft-0.4*sin(0.33*iTime) ) * fft;\n    value = (uv.y < 1.0) ? uv.y : uv.y - 1.0;\n    visualizer += 0.8 -  smoothstep( 0.0, 0.15, abs(wave - value) );"}