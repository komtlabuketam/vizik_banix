{"id": 3, "style": "Bar", "contentVariable": "", "contentFunction": "void pixellize(inout mediump vec2 uv, mediump float value){\n    uv *= value;\n    uv = floor(uv);\n    uv /= value;\n}", "contentVisualizer": "uv = (2.0 * gl_FragCoord.xy - iResolution.xy) / iResolution.y;\n    uv *= 0.566;\n    uv.x = abs(uv.x);\n    uv.y += 0.882;\n    highp vec4 colVisual1 = vec4(0.07, 0.12, 0.35, 1.0);\n    highp vec4  colvisual3 = vec4(0.99, 0.0, 1.0, 1.0);\n    highp float bars = 1.0 - abs(mod(uv.x * 320.0, 2.0) - 1.0);\n    pixellize(uv, 80.0);\n    highp float aud = step(uv.y, texture2D(iChannel0, vec2(uv.x, 0.0)).r);\n    highp vec4 col = mix(vec4(0.0), colVisual1, bars * aud);\n    col = mix(col, colvisual3, col.w);\n    visualizer = vec3(col.xyz);"}