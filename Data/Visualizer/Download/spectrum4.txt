{"id": 4, "style": "Spectrum", "contentVariable": "", "contentFunction": "mediump float sdCircle( in mediump vec2 p, in mediump float r ) {\n    return length(p)-r;\n}", "contentVisualizer": "uv = (2.*gl_FragCoord.xy - iResolution.xy)/min(iResolution.x,iResolution.y);\n    highp float a = abs(atan(uv.y, uv.x))/PI;\n    highp float w0 = texture2D(iChannel0, vec2(a, 0)).x;\n    highp float d0 = (abs(sdCircle(uv, .652))-.15*w0)*40.;\n    visualizer += mix(vec3(.98,.76,.67), vec3(.25,.86,.89), w0*.75)*(1.-fract(d0))*exp(-.46*d0);"}