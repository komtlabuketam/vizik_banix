{"id": 12, "style": "Spectrum", "contentVariable": "", "contentFunction": "", "contentVisualizer": "mediump float time = iTime;\n    const mediump float pi = 3.14159;\n    const mediump float pi2 = 3.14159 * 2.;\n    mediump float aspect = iResolution.x / iResolution.y;\n    uv = gl_FragCoord.xy / iResolution.xy;\n    mediump vec2 uvViewport = uv * 2. - 1.;\n    uvViewport.x *= aspect;\n    mediump float angle = atan(uvViewport.y, uvViewport.x) * 3.;\n    mediump float radius = (length(uvViewport) - 0.1);\n    mediump vec2 uvPolar = vec2(angle, radius);\n    uvPolar.x = mod(abs(sin(uvPolar.x)),1.);\n    uvPolar.y = uvPolar.y * 2. - 0.5;\n    mediump float thinSignal = 0.1;\n    mediump float plotter = -(thinSignal/(texture2D(iChannel0, uvPolar).r-uvPolar.y));\n    mediump float signP2 = sign(plotter);\n    plotter = abs(plotter);\n    mediump vec3 red = vec3(0.5,0.2,0.2);\n    mediump vec3 blue = vec3(0.1,0.2,0.5);\n    mediump vec3 yellow = vec3(0.9,0.6,0.2);\n    mediump vec3 gray = vec3(0.2,0.2,0.25);\n    mediump vec3 colorP = mix(red, gray, 1.);\n    colorP = mix(colorP, yellow, 1.-clamp(signP2,0.,1.));\n    visualizer = mix(visualizer, colorP, plotter);"}