{"id": 3, "style": "Star Field", "contentVariable": "const mediump vec3 cold = vec3(255.0, 244.0, 189.0)/255.0;\nconst mediump vec3 hot  = vec3(181.0, 236.0, 255.0)/255.0;", "contentFunction": "mediump vec4 hash42(mediump vec2 p){\n    mediump vec4 p4 = fract(vec4(p.xyxy) * vec4(.1031, .1030, .0973, .1099));\n    p4 += dot(p4, p4.wzxy+19.19);\n    return fract((p4.xxyz+p4.yzzw)*p4.zywx) - 0.5;\n}\n\nmediump vec2 hash22(mediump vec2 p){\n    mediump vec3 p3 = fract(vec3(p.xyx) * vec3(443.897, 441.423, 437.195));\n    p3 += dot(p3, p3.yzx+19.19);\n    return -1.0+2.0*fract((p3.xx+p3.yz)*p3.zy);\n}\n\nmediump float Gradient2D( in mediump vec2 p ){\n    mediump vec2 i = floor( p );\n    mediump vec2 f = fract( p );\n    mediump vec2 u = f*f*(3.0-2.0*f);\n    return mix( mix( dot( hash22( i + vec2(0.0,0.0) ), f - vec2(0.0,0.0) ),\n    dot( hash22( i + vec2(1.0,0.0) ), f - vec2(1.0,0.0) ), u.x),\n    mix( dot( hash22( i + vec2(0.0,1.0) ), f - vec2(0.0,1.0) ),\n    dot( hash22( i + vec2(1.0,1.0) ), f - vec2(1.0,1.0) ), u.x), u.y);\n}\n\nmediump vec3 StarFieldLayer(mediump vec2 p, mediump float du, mediump float count, mediump float brightness, mediump float size){\n    mediump vec2 pi;\n    du *= count;    p *= count;\n    pi  = floor(p); p  = fract(p)-0.5;\n    mediump vec4 h = hash42(pi);\n    mediump float s = brightness*(0.7+0.6*h.z)*smoothstep(0.8*du, -0.2*du, length(p+0.9*h.xy) - size*du);\n    return s*mix(mix(vec3(1.), cold, min(1.,-2.*h.w)), hot, max(0.,2.*h.w));\n}\n\nmediump vec3 StarField(mediump vec2 p, mediump float du){\n    mediump vec3 c;\n    c  = StarFieldLayer(p, du, 25.0, 0.18, 0.5);\n    c += StarFieldLayer(p, du, 15.0, 0.25, 0.5);\n    c += StarFieldLayer(p, du, 12.0, 0.50, 0.5);\n    c += StarFieldLayer(p, du,  5.0, 1.00, 0.5);\n    c += StarFieldLayer(p, du,  3.0, 1.00, 0.9);\n\n    mediump float s = 3.5*(max(0.2, Gradient2D(2.0*p*vec2(1.2,1.9)))-0.2)/(1.0-0.2);\n    c += s*StarFieldLayer(p, du, 160.0, 0.10, 0.5);\n    c += s*StarFieldLayer(p, du,  80.0, 0.15, 0.5);\n    c += s*StarFieldLayer(p, du,  40.0, 0.25, 0.5);\n    c += s*StarFieldLayer(p, du,  30.0, 0.50, 0.5);\n    c += s*StarFieldLayer(p, du,  20.0, 1.00, 0.5);\n    c += s*StarFieldLayer(p, du,  10.0, 1.00, 0.9);\n    c *= 1.3;\n    mediump float f = 1.0 / sqrt(660.0*du);\n    return f*min(c, 1.0);\n}", "contentParticle": "mediump float du = 1.0 / iResolution.y;\n    uv = du*(gl_FragCoord.xy-0.5*iResolution.xy) + 1.33 + iTime/30.0;\n    particle = vec3(StarField(uv, du));"}