{"id": 2, "style": "Matrix", "contentVariable": "const mediump float size = .3;\nconst mediump float thickness = .05;\nconst mediump float iterations = 15.;", "contentFunction": "mediump mat2 rotate(mediump float rads)\n{\n    return mat2(cos(rads), sin(rads), -sin(rads), cos(rads));\n}\n\nmediump float getDist(mediump vec2 uv)\n{\n    mediump vec2 coord = rotate(sin(iTime * .65) * 2.35) * abs(fract(uv) - .5);\n    return max(coord.x, coord.y);\n}", "contentParticle": "uv = 1.75 * (2. * gl_FragCoord.xy - iResolution.xy) / iResolution.y;\n    mediump vec2 stepAmt = uv * .005;\n    mediump float colAmt = 0.;\n\n    for (mediump float i = 0.; i < iterations; i++)\n    {\n        uv += stepAmt * i;\n        mediump float maxDist = getDist(uv);\n        colAmt += (smoothstep(size, size - .05, maxDist)\n        * smoothstep(size - thickness, size - (thickness - .05), maxDist));\n    }\n\n    particle = vec3(colAmt + (1. - step(0.2, colAmt)) * .1, 0., 0.);"}