{"id": 1, "contentVariable": "", "contentFunction": "mediump vec3 rule(mediump float time){\n    mediump vec3 rule;\n    rule.x = clamp((0.3334 - time) * 3.0, 0.0, 1.0);\n    rule.z = clamp((time - 0.6666) * 3.0, 0.0, 1.0);\n    rule.y = clamp(1.0 - rule.x - rule.z, 0.0, 1.0);\n    return rule;\n}", "contentTransition": "mediump float progress = translatePercent;\n        mediump vec3 src = get2DTexture(iChannel1, uv);\n        mediump vec3 dst = get2DTexture(iChannel3, uv);\n        mediump vec3 black = vec3(0.0);\n        mediump vec3 rule = rule(progress);\n        background = src * rule.x + black * rule.y + dst * rule.z;"}