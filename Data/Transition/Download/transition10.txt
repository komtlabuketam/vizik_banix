{"id": 10, "contentVariable": "", "contentFunction": "", "contentTransition": "mediump vec4 clrA = texture2D(iChannel1, uv);\n        mediump vec4 clrBG = texture2D(iChannel3, uv);\n        mediump float edge_width_start = 0.15;\n        mediump float edge_width_end = 0.05;\n        mediump float edge_width = mix(edge_width_start, edge_width_end, smoothstep(0., 1., translatePercent));\n        mediump float myAlpha = mix(0. - edge_width, 1., translatePercent);\n        mediump vec4 alphaTex = texture2D(iChannel3, uv * 0.4);\n        mediump float a = step(alphaTex.r, myAlpha);\n        mediump float edge = smoothstep(alphaTex.r - edge_width, alphaTex.r, myAlpha);\n        mediump vec4 edgeColor = vec4(0., 0.1, 1.0, 1.0);\n        edgeColor *= edge * 10.;\n        clrA += edgeColor;\n        background = mix(clrA, clrBG, a).rgb;"}