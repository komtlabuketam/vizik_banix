{"id": 18, "contentVariable": "", "contentFunction": "mediump mat2 scale(mediump vec2 _scale){\n    return mat2(_scale.x, 0.0, 0.0, _scale.y);\n}", "contentTransition": "mediump float trans = smoothstep(0., 0.75, sin(uv.x + 1. + (2.2 * translatePercent)));\n        mediump vec2 scaled = scale(vec2(1. + sin(fract(trans) * 3.14159265359))) * uv;\n        uv.x = scaled.x;\n        mediump vec3 img1= vec3(texture2D(iChannel1, uv, 0.));\n        mediump vec3 img2=  vec3(texture2D(iChannel3, uv, 0.));\n        mediump vec3 col = img1 * trans + img2 * (1.-trans);\n        background = col;"}